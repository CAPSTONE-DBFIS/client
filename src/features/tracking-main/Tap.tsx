import { Box } from '@/shared/ui/Box'
import * as style from './tap.css'

/**
 * 메인 콘텐츠 영역 중 캘린더/리스트 탭 선택
 *
 * @returns {JSX.Element}
 */
export const Tap = () => {
    return <Box className={style.layout}></Box>
}
