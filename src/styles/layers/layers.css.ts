import { globalLayer, layer } from '@vanilla-extract/css'

export const reset = globalLayer('reset')
export const components = layer('components')
// layer service 지원 브라우저 https://caniuse.com/css-cascade-layers
